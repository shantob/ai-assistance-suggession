# PERSONAL ADVICE — AI Desktop Assistant
# Requirements
#
# Core (tkinter, urllib, json, threading) ships with Python — nothing to
# install for the base app to run.

# Optional: only needed on Linux/Mac for precise idle-time detection.
# (Windows uses the built-in Win32 API instead — no extra package needed.)
pynput>=1.7.6

# Optional: only needed if you build a standalone .exe.
# pyinstaller>=6.0
